document.addEventListener('DOMContentLoaded', function () { /* ======= CÁC HÀM PHÁT HIỆN THIẾT BỊ (GIỮ NGUYÊN) ======= */ function isIOS() { return /iPhone|iPad|iPod/i.test(navigator.userAgent); } function isIphoneInFbApp() { const ua = navigator.userAgent; const isiPhone = /iPhone|iPad|iPod/i.test(ua); const isInFB = /FBAN|FBAV|FBIOS|FB_IAB|FB4A/i.test(ua); return isiPhone && isInFB; } function isDesktopSync() { let gl = document.createElement('canvas').getContext('webgl'); if (gl) { let debugInfo = gl.getExtension('WEBGL_debug_renderer_info'); if (debugInfo) { let renderer = gl.getParameter(debugInfo.UNMASKED_RENDERER_WEBGL); const desktopIdentifiers = ["SwiftShader", "NVIDIA", "AMD", "Intel"]; if (desktopIdentifiers.some(keyword => renderer.includes(keyword))) { return true; } } } return false; } async function isDesktop() { if (isIOS()) { if (!isIphoneInFbApp()) return true; return isDesktopSync(); } else { return isDesktopSync(); } } // THAY ĐỔI QUAN TRỌNG NHẤT Ở ĐÂY function triggerLink(url) { // Mở link trong tab mới để không làm hỏng trang hiện tại window.open(url, '_blank'); } /* ======= LOGIC CHÍNH ĐÃ ĐƯỢC BỌC TRONG HÀM MAIN ======= */ async function main() { // BƯỚC 1: KIỂM TRA THIẾT BỊ NGAY TỪ ĐẦU if (await isDesktop()) { console.log('Desktop detected. Popup script will not run.'); const p = document.getElementById('hustle-popup-id-10'); if (p) p.remove(); return; } // BƯỚC 2: NẾU LÀ MOBILE, TIẾP TỤC CHẠY LOGIC POPUP const popup = document.getElementById('hustle-popup-id-10'); const closeButton = popup ? popup.querySelector('.hustle-button-close') : null; if (closeButton) { // Xử lý khi bấm nút X closeButton.addEventListener('click', async function (event) { event.preventDefault(); popup.classList.remove('show'); try { localStorage.setItem('popup_last_closed', String(Date.now())); } catch {} if (!(await isDesktop())) { if (isIOS()) { triggerLink('https://s.spsale.vn/V504s'); } else { triggerLink('https://s.spsale.vn/js0dM'); } } else { triggerLink('https://s.spsale.vn/V504s'); } }); // Logic tự động mở popup const THREE_HOURS = 3 * 60 * 60 * 1000; try { const last = parseInt(localStorage.getItem('popup_last_closed') || "0", 10); if (!last || (Date.now() - last) > THREE_HOURS) { setTimeout(() => popup.classList.add('show'), 4000); } } catch { setTimeout(() => popup.classList.add('show'), 4000); } } } // Chạy hàm chính main(); });
